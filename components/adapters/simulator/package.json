{
  "name": "@plutolang/simulator-adapter",
  "version": "0.3.24",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pluto-lang/pluto.git",
    "directory": "components/adapters/simulator"
  },
  "author": "plutolang",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "watch": "npm run build -- --watch",
    "test": "NODE_ENV=test vitest run --coverage --reporter=verbose",
    "test:watch": "NODE_ENV=test vitest --coverage --reporter=verbose",
    "lint": "eslint ."
  },
  "dependencies": {
    "@plutolang/base": "workspace:^",
    "@plutolang/pluto": "workspace:^",
    "@plutolang/pluto-infra": "workspace:^",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/node": "^20.8.4",
    "@vitest/coverage-v8": "^0.34.6",
    "typescript": "^5.2.2",
    "vitest": "^0.34.6"
  }
}