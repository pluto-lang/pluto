import { Website } from "@plutolang/pluto";

function buildReactApp(projectPath: string) {
  // Compile the React project locally
  return distPath;
}

const projectPath = "path/to/your/project";
const distPath = buildReactApp(projectPath);

new Website(distPath, "website");
